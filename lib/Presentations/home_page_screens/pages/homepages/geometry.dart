import 'package:flutter/material.dart';

class geometri extends StatelessWidget {
  const geometri({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('geometry')),
    );
  }
}
