import 'package:flutter/material.dart';

class temperature extends StatelessWidget {
  const temperature({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Temperature')),
    );
  }
}
